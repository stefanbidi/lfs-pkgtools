# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description. Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in. You must
# make exactly 11 lines for the formatting to be correct. It's also
# customary to leave one space after the ':'.

          |-----handy-ruler------------------------------------------------------|
pkg-config: pkg-config ()
pkg-config:
pkg-config: The pkg-config package contains a tool for passing the include path
pkg-config: and/or library paths to build tools during the configure and make
pkg-config: file execution.
pkg-config:
pkg-config: Homepage: https://www.freedesktop.org/wiki/Software/pkg-config
pkg-config:
pkg-config:
pkg-config:
pkg-config:
